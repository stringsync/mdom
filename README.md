# mdom

A DOM for MusicXML.

## Development

Use the `mdom` command-line tool for development.

To install:

```sh
bun link
```

Run the test suite:

```sh
mdom test
```

Format, lint, and typecheck:

```sh
mdom fix
```
